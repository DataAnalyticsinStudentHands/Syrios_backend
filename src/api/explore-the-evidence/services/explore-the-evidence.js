'use strict';

/**
 * explore-the-evidence service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::explore-the-evidence.explore-the-evidence');
